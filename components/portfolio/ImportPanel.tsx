'use client';

// Import de CSV (Flujo 3, 2 fases) — vive en el comparativo `/` como botón + panel.
// B0 sólo reserva el punto de montaje (la página ya lo referencia); B5 implementa
// acá el botón "Importar CSV", la subida (fase 1) y la resolución de conflictos
// campo por campo (fase 2). Hasta entonces no renderiza nada.
export function ImportPanel() {
  return null;
}
